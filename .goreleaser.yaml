project_name: kov

before:
  hooks:
    - go mod tidy

builds:
  - id: kov
    main: ./cmd/kov
    binary: kov
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.ShortCommit}}
      - -X main.date={{.Date}}

archives:
  - id: default
    format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: 'checksums.txt'

snapshot:
  name_template: "{{ .Tag }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^ci:'
      - '^chore:'

release:
  github:
    owner: up1512001
    name: kov
  draft: false
  prerelease: auto
  name_template: "Kov {{.Version}}"

nfpms:
  - id: kov
    package_name: kov
    homepage: "https://trykov.dev"
    description: "Indestructible AI coding in your terminal"
    maintainer: "Utsav <hello@trykov.dev>"
    license: MIT
    formats:
      - deb
      - rpm

# Homebrew tap (macOS / Linux)
# Requires: create repo github.com/up1512001/homebrew-tap
brews:
  - name: kov
    repository:
      owner: up1512001
      name: homebrew-tap
      token: "{{ .Env.GITHUB_TOKEN }}"
    homepage: "https://trykov.dev"
    description: "Indestructible AI coding in your terminal"
    license: MIT
    install: |
      bin.install "kov"
    test: |
      system "#{bin}/kov", "version"

# Scoop bucket (Windows)
# Requires: create repo github.com/up1512001/scoop-bucket
scoops:
  - name: kov
    repository:
      owner: up1512001
      name: scoop-bucket
      token: "{{ .Env.GITHUB_TOKEN }}"
    homepage: "https://trykov.dev"
    description: "Indestructible AI coding in your terminal"
    license: MIT
